<script lang="ts">
</script>

# 输出格式规范

AI Agent 的输出格式直接影响用户体验。一套好的格式规范能让回复既清晰可扫描，又不显得机械。

## 核心原则：格式服务于内容

Codex 的格式规范有一个贯穿始终的原则：

> 让格式帮助结果易于扫描，但不要让它显得机械。用判断力决定多少结构真正有帮助。

这意味着不是所有回复都需要标题和列表。一个简单问题可以用一句话回答，复杂任务才需要分节组织。

## Markdown 使用规则

**标题**

标题是可选的，只在真正有帮助时使用。使用时：
- 1~3 个词，Title Case
- 用 `**…**` 包裹（粗体）而非 `#` 标记
- 不在标题后加空行

**列表**

- 永远不嵌套列表，保持单层
- 如果需要层级，拆分成独立的列表或段落
- 有序列表只用 `1. 2. 3.` 风格，不用 `1)`

**内联代码**

命令、文件路径、环境变量、代码标识符都用反引号包裹。这是最频繁使用的格式标记，比标题和列表更有信息密度。

## 文件引用格式

Codex 对文件引用有专门规定，要求生成可点击的链接：

```
[app.py](/abs/path/app.py:12)
```

规则：
- 使用绝对路径
- 可选添加行号（`:12`）
- 路径中有空格时用尖括号包裹目标
- 不用 `file://`、`vscode://` 等 URI 前缀
- 不用反引号包裹链接

**对比：gpt-5.2 vs gpt-5.5 的文件引用规范**

gpt-5.2 允许相对路径、`a/` 或 `b/` diff 前缀、裸文件名：
> `src/app.ts`, `src/app.ts:42`, `b/server/index.js#L10`

gpt-5.5 更严格，要求绝对路径，格式为 `[label](/abs/path:line)`。

两种格式都指向同一目标：让文件引用在 CLI 或编辑器中可以点击跳转，而不是普通文本。

## 简洁度要求

**最终回复的长度规范**

Codex 按任务规模分了四档：

- 微小/单文件改动（≤10 行）：2~5 句话或 ≤3 条列表，无标题，0~1 个短代码片段
- 中等改动（单区域或几个文件）：≤6 条列表或 6~10 句话，至多 1~2 个短片段
- 大型/多文件改动：每文件 1~2 条列表，除非关键否则不内联代码
- 任何规模都不超过 50~70 行

**禁止事项**

- 不用"Done —"、"Got it"、"Great question" 等开场白
- 不用"If you want to..."结尾
- 不说"save/copy this file"（用户和你在同一台机器上）
- 不在最终回复中写 before/after 对比、完整方法体、大段滚动代码

## 版本迭代中的格式演化

对比 `codex.pretty.prompt.json` 中 gpt-5.2 和 gpt-5.5 的格式规范，可以发现几个演化趋势：

1. **更细的长度分档**：gpt-5.2 只说"简洁，不超过 10 行"，gpt-5.5 按任务规模给出了量化的上限
2. **更严格的文件链接格式**：从允许多种格式收敛为统一的绝对路径格式
3. **人格融入格式**：gpt-5.5 明确要求"最终回复的语气必须匹配人格"，将格式规范和人格规范打通

这说明格式规范不是一次性写好的，而是随着实际使用中的问题反馈不断收紧和细化的。
