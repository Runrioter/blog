<script lang="ts">
</script>

# 基础概念

提示词（Prompt）是你与 AI 模型沟通的语言。写好提示词的前提是理解它的基本构成。

## 提示词的三个层次

一个完整的提示词通常由三层构成：

**System Prompt（系统提示）**

在对话开始前由开发者写入，用于定义模型的角色、能力边界和行为规范。用户通常看不到这一层。它是提示词工程中最关键的部分，决定模型"是谁"。

**User Prompt（用户提示）**

用户在对话中实际输入的内容。质量越高，模型的回答越准确。清晰、具体、有上下文的用户提示能大幅提升输出质量。

**Assistant Prompt（助手提示）**

模型的回复。在 few-shot 场景下，开发者有时会预填一部分助手回复，引导模型按特定格式或风格继续输出。

## Role / Context / Instruction 模型

描述一个提示词任务时，三要素缺一不可：

- **Role**：模型扮演什么角色？（例如"你是一个资深软件工程师"）
- **Context**：当前的背景是什么？（例如"这是一个 SvelteKit 项目，使用 Svelte 5 runes 语法"）
- **Instruction**：具体要做什么？（例如"帮我把这个组件改成 TypeScript"）

这三者共同构成一个有效的提示。缺少 Role，模型回答会偏泛；缺少 Context，回答可能不贴合实际；缺少 Instruction，模型不知道该输出什么。

## 提示词的两种主要形态

**Zero-shot**：直接告诉模型任务，不提供例子。适合简单、通用的任务。

**Few-shot**：在提示词中附上 1~3 个"输入→输出"的示例，让模型模仿格式或推理方式。对于格式敏感或逻辑复杂的任务效果显著。

## 温度与确定性

`temperature` 控制输出的随机性。越高越有创意，越低越稳定可预测。写代码、填表格时用低温度（0.0~0.3）；写文案、头脑风暴时可以适当调高（0.7~1.0）。

大多数 Agent（如 Codex、VS Code Copilot）默认使用低温度，因为它们需要产出可执行的、确定性强的结果。
