<script lang="ts">
</script>

# Agent 行为设计

Agent 和普通聊天机器人的核心区别在于：Agent 能主动使用工具、持续执行多步骤任务。这对提示词提出了更高的要求。

## 自主性与持续性

Codex 的系统指令中有一段专门的"Autonomy and Persistence"规范：

> 在当前轮次内尽可能端到端地完成任务：不要止步于分析或半成品修复；除非用户明确暂停或重定向，否则要将工作贯穿实现、验证和结果说明的全过程。

这段话揭示了 Agent 提示词的一个关键设计原则：**明确告诉模型什么时候停、什么时候继续**。如果不写清楚，模型容易在"输出一个方案"就停下，而不是真正去执行。

对应的触发条件也要写清楚：

> 除非用户明确要求一个计划、提问、或正在头脑风暴，否则假设用户希望你直接做出代码变更或运行工具。

## 双通道通信模型

Codex 的提示词设计了两个通信通道：

- **commentary**（评论通道）：工作过程中的中间更新，每 30 秒一次，告知用户当前在做什么
- **final**（最终通道）：任务完成后的总结回复

这个设计解决了一个实际问题：长时间运行的 Agent 任务如果没有中间反馈，用户会不知道它是在认真工作还是卡死了。提示词中明确规定：

> 中间更新是你边工作边思考的方式，以平静、陪伴的方式随口说明你在做什么、为什么。

好的中间更新的特征：
- 1~2 句话，不超过
- 说明正在收集什么上下文、学到了什么
- 句式结构多样，避免每次都以同样方式开头

## 工具使用规范

Agent 提示词通常要明确规定工具的使用方式，避免模型做出危险或低效的操作。以 Codex 为例：

**文件操作**
- 手动编辑用 `apply_patch`，不用 `cat` 或其他 shell 写入技巧
- 不用 Python 读写文件，除非 shell 命令做不到

**搜索**
- 优先用 `rg`（ripgrep），因为比 `grep` 快得多
- 可以并行化文件读取：`cat`、`rg`、`sed`、`ls` 等可以同时发起

**Git 操作**
- 永远不用 `git reset --hard` 或 `git checkout --` 除非用户明确要求
- 不用 git 交互式控制台，始终选择非交互命令

这类规范的作用是**把团队的最佳实践硬编码进工具调用层**，减少 Agent 凭直觉选错工具的概率。

## 状态管理与计划追踪

对于复杂任务，部分 Agent 实现（如 gpt-5.2）提供了 `update_plan` 工具，用于实时追踪任务步骤。提示词中对此有严格规定：

- 同一时刻只能有一个步骤处于 `in_progress` 状态
- 不能将步骤从 `pending` 直接跳到 `completed`，必须先设为 `in_progress`
- 不能在事后批量标记完成，要即时更新
- 理解变化时（拆分/合并/重排步骤），先更新计划再继续编码

这些规则防止计划追踪变成摆设。计划的价值在于让用户随时能看到 Agent 当前在哪、下一步要去哪。

## 处理意外变更

Agent 在工作中经常会遇到它没有做过的变更（用户在另一个窗口改了文件，或者工具自动生成了输出）。Codex 对此有明确的处理策略：

> 这些变更可能来自用户或自动生成。如果与当前任务无关，忽略它们。如果影响了当前任务，与这些变更协作而不是撤销它们。只有当这些变更让任务无法完成时，才询问用户如何处理。
