<script lang="ts">
</script>

# 系统指令设计

系统指令（System Instructions）是 Agent 提示词的核心。以 Codex 的 `base_instructions` 为例，一份成熟的系统指令通常包含以下模块。

## 人格（Personality）

人格模块定义模型的性格基调，影响它在模糊情境下的行为选择。Codex 提供了两种预设人格：

**Pragmatic（务实型）**

```
你是一个务实、高效的软件工程师。
你的核心价值观：
- 清晰：明确陈述推理和权衡，方便事前评估
- 务实：聚焦目标和动力，专注于实际可行的方案
- 严谨：论点要连贯可辩护，温和指出假设中的漏洞
```

**Friendly（友好型）**

```
你优化的是团队士气和支持感，而非仅仅是代码质量。
你用"我们"和"一起"这样的语言，肯定进展，将评判替换为好奇。
```

两种人格都不鼓励过度解释或空洞的鼓励语，区别在于沟通的情感温度。选择哪种取决于你的目标用户群体。

## 值观与交互风格

人格模块通常紧跟 **Values** 和 **Interaction Style** 两个子节，用更具体的行为规则来约束人格落地：

- Values 列举 2~4 个核心价值，每个配一句话说明如何体现
- Interaction Style 描述模型如何"说话"——比如"不评论用户请求的好坏，除非有升级理由"

这种结构避免了人格描述过于抽象，让模型有可操作的锚点。

## 升级策略（Escalation）

当决策有不明显后果或隐藏风险时，模型应主动暂停并告知用户，而不是直接执行。好的升级规则有两个特征：

1. **有触发条件**，不是"遇到任何不确定时都问"
2. **以支持而非纠错的语气**呈现，比如"我想确认一下，避免走错方向"

务实型 Codex 的升级规则写法：
> 你可以挑战用户提高技术标准，但绝不能居高临下或无视他们的顾虑。

友好型的写法：
> 升级以支持和共同担责的方式呈现——永远不是纠正——引入一个明确的暂停来重新对齐。

## 通用工程规范（General）

人格之后，系统指令通常包含一段通用工程规范，规定模型在代码层面的默认行为：

- 优先用现有代码库的模式，而非发明新抽象
- 对结构化数据使用解析器而非字符串操作
- 测试覆盖率与变更的风险和影响范围成正比
- 搜索文件时首选 `rg`（ripgrep），而非 `grep`

这类规范的价值在于**减少模型在每次任务中重新推断"惯例"的开销**，它直接把团队的工程文化编码进提示词。

## 模块化组合

Codex 的 `instructions_template` 用 `{{ personality }}` 作为占位符，在运行时动态插入不同的人格段落。这是一种很实用的设计：

```
你是 Codex，一个基于 GPT-5 的编程助手。
{{ personality }}
# 通用规范
...
```

这样同一套核心规范可以适配不同的人格风格，维护时只需修改一处，不用到处同步。
